<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to receive boot completed broadcast for auto-start service -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Permission to request ignoring battery optimizations (keep service alive) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- BirthdayKeeper: exact alarm scheduling (Android 14+ requires manual grant) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- BirthdayKeeper: wake lock for alarm activity -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- BirthdayKeeper: full screen intent for alarm overlay -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- BirthdayKeeper: overlay window for alarm popup when screen is on -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Android 11+ package visibility: needed to query other apps' icons -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:name=".AppMessageCaptureApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppMessageCapture"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppMessageCapture"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 账单快捷编辑弹窗：从「记账成功」通知的 完善账单 按钮打开 -->
        <activity
            android:name=".ui.BillQuickEditActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.AppMessageCapture.QuickEdit" />

        <!-- 快捷编辑悬浮窗入口（已授予悬浮窗权限时的通知按钮通道） -->
        <receiver
            android:name=".ui.BillQuickEditOverlayReceiver"
            android:exported="false" />

        <!-- Notification Listener Service -->
        <service
            android:name=".service.MessageCaptureService"
            android:exported="true"
            android:label="@string/service_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- 屏幕记账无障碍服务：读取支付成功页金额（京东等不发通知的支付场景） -->
        <service
            android:name=".service.PaymentScreenAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/payment_screen_accessibility_config" />
        </service>

        <!-- BirthdayKeeper: widget click trampoline activity (no UI) -->
        <activity
            android:name=".birthday.widget.BirthdayWidgetActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- BirthdayKeeper: full screen alarm activity -->
        <activity
            android:name=".birthday.ui.AlarmActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar.Fullscreen" />

        <!-- BirthdayKeeper: alarm trigger receiver -->
        <receiver
            android:name=".birthday.service.BirthdayAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Broadcast receiver for boot completion to auto-start the service -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- BirthdayKeeper: re-schedule alarms after reboot -->
        <receiver
            android:name=".birthday.service.BirthdayBootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- BirthdayKeeper: home screen widget -->
        <receiver
            android:name=".birthday.widget.BirthdayWidgetReceiver"
            android:enabled="true"
            android:exported="false"
            android:label="生日管家">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/birthday_widget_info" />
        </receiver>

        <!-- FileProvider for exporting CSV/JSON files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
