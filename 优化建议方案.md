## AppMessageCapture 优化与新增功能建议方案

> 基于项目源码全面分析 + 国内外主流记账/工具软件调研，整理以下建议供决策。

---

### 一、现有代码与性能优化（建议优先处理）

**1. 数据库查询性能**

当前 `NotificationViewModel` 中 `todayCount` 的实现方式是先把全部通知加载到内存，再在 Kotlin 层过滤计数。随着数据量增长会越来越慢。建议在 DAO 层添加 `SELECT COUNT(*) FROM notifications WHERE timestamp >= :startOfDay` 这样的 SQL 查询。同样的问题也存在于 `ReportViewModel.loadData()`——它用 `getAllBillsOnce()` 全量拉取账单后在内存中按周期过滤，建议改为 SQL 层面带时间范围的查询。

**2. 跨午夜数据过期**

`BillViewModel` 中 `startOfDay` 在 ViewModel 构造时计算一次就固定不变了。如果用户不杀进程跨过午夜，"今日支出/收入"数据会停留在前一天。可以通过监听 `LocalTime.now()` 或使用 `LaunchedEffect` 在午夜时自动刷新。

**3. 文件拆分与代码组织**

几个核心 Screen 文件体积过大（`BillScreen.kt` 54.5KB / `ReportScreen.kt` 43.6KB / `MainScreen.kt` 41KB），建议按 Composable 组件拆分到独立文件，便于维护和复用。此外 `BillScreen.kt` 中存在一个已废弃的 `BentoStatsGrid` 函数（死代码），建议清理。

**4. 清理无用依赖与硬编码**

`build.gradle.kts` 引入了 Vico Charts 1.14.0，但报表图表全部是 Canvas 手绘实现的，Vico 实际未使用，白白增加了 APK 体积。建议移除。同时大量 `Color(0xFFXXXXXX)` 硬编码颜色值建议统一纳入 Theme 的 ColorScheme 管理，方便后续换肤或暗色模式适配。

**5. PendingIntentCache 无上限**

当前使用 `ConcurrentHashMap` 缓存通知的 PendingIntent，没有大小限制也没有 LRU 淘汰策略，长时间运行可能内存泄漏。建议加一个上限（比如保留最近 200 条）或使用 `LruCache`。

**6. blockedApps 功能缺失 UI 入口**

`PreferencesManager` 已经实现了完整的 block/unblock API，但 UI 中没有任何管理屏蔽列表的界面，用户无法操作。建议补一个"屏蔽管理"页面。

**7. 数据库 schema 导出**

当前 `exportSchema = false`，无法利用 Room 的 schema 验证功能。建议开启并配合自动迁移测试。

---

### 二、记账模块功能增强

结合随手记、钱迹、咔皮记账、YNAB、Monarch Money、Copilot Money 等成熟产品的功能，以下是当前记账模块最值得补充的能力：

**1. 预算管理系统（高优先级）**

这是几乎所有成熟记账软件的标配功能，当前项目完全缺失。建议实现：

- 设置月度总预算和各分类预算
- 预算进度条可视化（已用/剩余）
- 超支预警通知推送
- 可选的"预算翻滚"机制（Copilot Money 的创新：未花完的预算滚存到下月）

实现思路：新增 `budgets` 表（category, amount, period, rollover），在 ReportScreen 中增加预算视图。

**2. 手动记账增强**

当前手动记账只有金额和分类，建议增加：

- **备注字段**：给每笔账单添加文字备注说明
- **日期选择**：手动记账时可以选择过去或未来的日期（补记/预记）
- **周期性账单**：固定支出（房租、会员、话费）设为自动周期记账，参考百事AA记账的定时周期功能

实现思路：`BillEntity` 添加 `note: TEXT` 和 `recurrenceRule: TEXT`（null 表示一次性），`BillDao` 添加对应查询。

**3. 账单搜索功能**

当前账单列表没有搜索功能（通知模块有，但记账模块没有）。建议添加按关键词、金额范围、日期范围、分类的组合搜索。

**4. 资产/账户管理**

参考随手记和 Monarch Money 的账户体系：

- 支持添加多个账户（银行卡、微信、支付宝、现金等）
- 记录账户初始余额
- 每笔账单关联支付账户
- 首页展示各账户余额汇总

实现思路：新增 `accounts` 表和 `AccountEntity`，`BillEntity` 添加 `accountId` 外键。

**5. AI 智能记账（进阶功能）**

咔皮记账和智懂记的核心竞争力。结合本项目的通知捕获能力，可以进一步增强：

- **短信/通知自动解析**：银行消费短信、支付宝/微信支付完成通知自动提取金额和商户
- **语音记账**：通过 Android SpeechRecognizer 说"午饭花了28块"自动解析
- **智能分类学习**：根据用户修改分类的历史，自动优化后续分类猜测的准确率

**6. 数据导出与备份**

当前只有通知模块支持 JSON/CSV 导出，账单模块没有。建议：

- 账单数据导出为 CSV（可导入 Excel 或其他记账软件）
- 账单数据导出为 JSON（完整备份）
- 支持从其他记账软件导入（随手记、挖财等的 CSV 格式）

**7. 记账桌面小组件**

生日模块已经有 Glance Widget 了，记账模块也可以做一个：

- 展示今日收支汇总
- 快捷记账按钮（点击打开记账弹窗）
- 本月预算进度

---

### 三、通知模块功能增强

**1. 应用屏蔽管理 UI**

前面已提到，`PreferencesManager` 已有 API 但无 UI。建议添加一个"应用管理"页面，列出所有发送过通知的应用，用户可以开关捕获。

**2. 通知收藏/星标**

重要通知（验证码、订单确认、快递通知等）可以收藏，方便日后查找，不被误删。

**3. 智能分类标签**

参考叨叨记账的精细分类思路，对通知自动打标签：验证码、快递物流、社交通讯、营销推广、系统提醒等，方便用户快速筛选。

**4. 通知定时清理**

设置自动清理规则：超过 N 天的通知自动删除，或保留指定应用的通知更长时间。

---

### 四、生日模块功能增强

**1. 倒数日/纪念日**

生日管家模块已有完整的倒数计算能力，可以低成本扩展为通用的"倒数日"功能：

- 支持添加任意纪念日（结婚纪念日、入职周年、还贷倒数等）
- 支持正数（已过天数）和倒数（剩余天数）两种模式
- 支持自定义重复周期（每年/每月/自定义间隔）

**2. 生日贺卡/礼物记录**

- 记录每年送了什么礼物、花了多少钱
- 生成送礼历史时间线
- 与记账模块联动（送礼支出自动归类到"人情往来"分类）

**3. 星座/生肖显示**

基于出生日期自动计算并显示星座和生肖，增加趣味性。

---

### 五、全局体验优化

**1. 暗色模式适配**

当前只有亮色主题，建议添加 Dark Mode 支持。Material 3 + Compose 的 `darkColorScheme` 可以较低成本实现。需要注意 Canvas 自绘图表的颜色也要适配。

**2. 应用锁/隐私保护**

记账和通知都是隐私数据，建议添加：

- 应用锁（PIN/指纹/面部识别）
- 使用 AndroidX Biometric 库
- 敏感通知内容模糊显示

**3. 数据概览首页**

参考 Monarch Money 和 Copilot Money 的 Dashboard 设计，将三个模块的数据整合到一个"总览"页面：

- 今日通知数 + 今日收支 + 最近生日
- 本月收支趋势迷你图
- 本月预算进度条

**4. 多语言支持**

当前只有中文，如果要开源或发布海外市场，建议添加 `strings.xml` 的英文翻译。

**5. 动态主题/换肤**

当前固定橙色+紫色配色，可以提供几套预设主题让用户选择，或者支持自定义主题色。

---

### 六、技术架构优化（长期）

**1. 引入依赖注入**

当前所有 ViewModel 直接调用 `AppDatabase.getDatabase(application)` 获取 DAO，耦合较重。建议引入 Hilt 或 Koin，方便测试和模块解耦。

**2. 引入 Navigation Compose**

当前通过 Tab 状态和 sealed class 路由管理页面跳转，随着功能增多会越来越复杂。建议引入 `navigation-compose` 统一管理。

**3. 补充单元测试**

当前无任何测试代码。建议至少覆盖：

- BillDao / NotificationDao / BirthdayDao 的查询逻辑
- 账单解析正则（`tryExtractBill` 中的关键词匹配和金额提取）
- 农历/公历生日计算（`DateCalculator`）
- 账单去重合并逻辑

**4. 数据库版本 v1->v2 迁移缺失**

迁移链从 v2 开始，如果有用户从 v1 升级会丢失数据。建议补充 v1->v2 的 Migration 或在低版本强制清理重建。

---

### 优先级建议

| 优先级 | 项目 | 预估工作量 |
|--------|------|------------|
| P0-紧急 | 数据库查询性能优化、跨午夜数据过期修复 | 小 |
| P0-紧急 | 移除无用 Vico Charts 依赖、清理死代码 | 小 |
| P1-重要 | 预算管理系统 | 中 |
| P1-重要 | 账单搜索、手动记账增强（备注+日期选择） | 中 |
| P1-重要 | 应用屏蔽管理 UI | 小 |
| P1-重要 | 账单数据导出（CSV/JSON） | 小 |
| P2-计划 | 资产/账户管理 | 大 |
| P2-计划 | 暗色模式适配 | 中 |
| P2-计划 | 记账桌面小组件 | 中 |
| P2-计划 | 倒数日/纪念日扩展 | 中 |
| P2-计划 | 周期性账单 | 中 |
| P3-远期 | AI 智能记账（语音/短信解析） | 大 |
| P3-远期 | 应用锁/隐私保护 | 中 |
| P3-远期 | 数据概览首页 | 中 |
| P3-远期 | 依赖注入 + Navigation Compose 重构 | 大 |
