# 应用消息捕获 — 完整运行清单

## 一、项目简介

本项目是一个完整的 Android 应用程序，名为 **应用消息捕获**。它是一个集通知捕获、智能记账、生日管理于一体的本地化效率工具。核心功能包括：

- 通过 `NotificationListenerService` 捕获所有应用通知，并自动识别支付类通知实现自动记账
- 手动记账，支持日期选择、分类管理、周期性账单
- 收支报表，含趋势图、分类构成环形图、分类详情下钻
- 生日管理，支持阳历/农历、到期提醒、桌面小组件、批量导入导出

UI 采用 `Jetpack Compose` + `Material Design 3` 构建，三栏 Tab 底部导航（消息 / 记账 / 生日），所有数据纯本地存储。

## 二、项目结构

```
AppMessageCapture/
├── build.gradle.kts                          // 项目级构建配置
├── settings.gradle.kts
├── gradle.properties
├── gradle/wrapper/gradle-wrapper.properties  // Gradle 8.7
├── README.md
├── 运行清单.md                                // 本文件
├── scripts/simulate_notification.sh          # 模拟支付通知自测（debug 专用）
├── scripts/migrate_old_app.sh                # 旧包名 app 数据一键迁移（debug 专用）
└── app/
    ├── build.gradle.kts                      // 模块级构建配置
    ├── proguard-rules.pro
    └── src/main/
        ├── AndroidManifest.xml
        ├── java/com/aifactory/appmessagecapture/
        │   ├── MainActivity.kt                      // 入口 Activity（三栏 Tab 导航）
        │   ├── AppMessageCaptureApplication.kt      // Application（初始化 Worker）
        │   │
        │   ├── data/                                # 通用数据层
        │   │   ├── AppDatabase.kt                   # Room 数据库（v11，含完整迁移链）
        │   │   ├── NotificationEntity.kt            # 通知消息实体
        │   │   ├── NotificationDao.kt               # 通知 DAO
        │   │   ├── BillEntity.kt                    # 账单实体
        │   │   ├── BillDao.kt                       # 账单 DAO
        │   │   ├── RecurringBillEntity.kt           # 周期账单实体
        │   │   ├── RecurringBillDao.kt              # 周期账单 DAO
        │   │   └── Categories.kt                    # 分类常量 + 迁移映射
        │   │
        │   ├── service/                             # 服务层
        │   │   ├── MessageCaptureService.kt         # 通知监听 + 自动记账
        │   │   └── BootReceiver.kt                  # 开机自启
        │   │
        │   ├── worker/                              # 后台任务
        │   │   └── RecurringBillWorker.kt           # 周期账单定时执行
        │   │
        │   ├── ui/                                  # 记账 UI 层
        │   │   ├── BillScreen.kt                    # 账单主界面（列表 + 手动记账 + 日期选择器）
        │   │   ├── BillViewModel.kt                 # 账单 ViewModel
        │   │   ├── RecurringBillScreen.kt           # 周期账单界面
        │   │   ├── RecurringBillViewModel.kt        # 周期账单 ViewModel
        │   │   ├── CategoryMigrationScreen.kt       # 分类迁移界面
        │   │   ├── MainScreen.kt                    # 消息主界面
        │   │   ├── NotificationViewModel.kt         # 通知 ViewModel
        │   │   ├── components/                      # 通用 UI 组件
        │   │   ├── report/                          # 收支报表模块
        │   │   │   ├── ReportScreen.kt              # 报表主界面（趋势图 + 环形图 + 分类列表）
        │   │   │   ├── ReportViewModel.kt           # 报表 ViewModel
        │   │   │   ├── CategoryDetailScreen.kt      # 分类详情下钻
        │   │   │   └── CategoryDetailViewModel.kt
        │   │   ├── setup/                           # 引导页
        │   │   └── theme/                           # 主题配色
        │   │       ├── Color.kt
        │   │       ├── Theme.kt
        │   │       └── Type.kt
        │   │
        │   ├── birthday/                            # 生日管理模块
        │   │   ├── data/
        │   │   │   ├── BirthdayEntity.kt            # 生日实体
        │   │   │   └── BirthdayDao.kt               # 生日 DAO
        │   │   ├── logic/
        │   │   │   ├── DateCalculator.kt            # 日期/倒计时计算
        │   │   │   └── LunarCalendarAdapter.kt      # 农历适配
        │   │   ├── service/
        │   │   │   ├── BirthdayAlarmScheduler.kt    # 闹钟调度
        │   │   │   ├── BirthdayAlarmReceiver.kt     # 闹钟广播接收
        │   │   │   └── BirthdayBootReceiver.kt      # 开机重设闹钟
        │   │   ├── ui/
        │   │   │   ├── BirthdayScreen.kt            # 生日导航入口
        │   │   │   ├── BirthdayListScreen.kt        # 生日列表（分页加载）
        │   │   │   ├── BirthdayEditScreen.kt        # 新建/编辑生日
        │   │   │   ├── BirthdayViewModel.kt         # 生日 ViewModel
        │   │   │   ├── AlarmActivity.kt             # 闹钟弹窗 Activity
        │   │   │   └── components/
        │   │   │       └── BirthdayCard.kt          # 生日卡片组件
        │   │   ├── utils/
        │   │   │   ├── BackupManager.kt             # JSON 导入导出
        │   │   │   └── PermissionHelper.kt          # 权限辅助
        │   │   └── widget/
        │   │       ├── BirthdayWidget.kt            # Glance 桌面小组件
        │   │       ├── BirthdayWidgetReceiver.kt    # 小组件接收器
        │   │       ├── BirthdayWidgetActivity.kt    # 小组件配置
        │   │       └── BirthdayWidgetWorker.kt      # 小组件定时刷新
        │   │
        │   └── utils/                               # 工具类
        │       ├── PreferencesManager.kt            # 偏好设置
        │       └── NotificationServiceHelper.kt     # 服务状态检测
        │
        └── res/
            ├── values/strings.xml, colors.xml, themes.xml
            ├── drawable/                            # 应用图标 + 20 个分类图标
            │   ├── ic_launcher_background.xml
            │   ├── ic_launcher_foreground.xml
            │   ├── ic_category_food.xml             # 分类图标（餐饮、交通、购物等）
            │   ├── ic_category_transport.xml
            │   └── ...
            ├── mipmap-anydpi-v26/
            │   ├── ic_launcher.xml
            │   └── ic_launcher_round.xml
            └── xml/
                ├── birthday_widget_info.xml         # 小组件配置
                ├── data_extraction_rules.xml
                └── backup_rules.xml
```

## 三、依赖环境

### 必需软件

1. **Android Studio**（推荐 Ladybug 或更新版本）
   - 下载地址：https://developer.android.com/studio
2. **JDK 17**（或更高版本）
   - Android Studio 自带 bundled JDK，通常无需单独安装
3. **Android SDK**
   - 通过 Android Studio 的 SDK Manager 安装
   - 必需组件：
     - Android SDK Platform 35
     - Android SDK Build-Tools 35
     - Android Emulator（如需在模拟器上运行）

### 核心依赖库

| 依赖 | 版本 | 用途 |
|------|------|------|
| Jetpack Compose BOM | 2025.02.00 | 声明式 UI 框架 |
| Material Design 3 | — | 现代化 UI 组件 |
| Room | 2.6.1 | 本地数据库持久化 |
| WorkManager | 2.9.1 | 周期任务调度（周期账单） |
| Glance | 1.1.1 | 桌面小组件 |
| Gson | 2.11.0 | JSON 序列化（导入导出） |
| Timber | 5.0.1 | 日志框架 |
| lunar | 1.7.7 | 农历日期计算 |
| KSP | 2.0.21-1.0.27 | 注解处理 |

### 系统要求

- **操作系统**：Windows 10/11、macOS 或 Linux
- **内存**：至少 8GB RAM（推荐 16GB）
- **磁盘空间**：至少 10GB 可用空间

## 四、如何运行该项目

### 步骤 1：打开项目

1. 启动 **Android Studio**
2. 选择 **Open**（打开现有项目）
3. 导航到本项目的根目录 `AppMessageCapture/`，点击 **OK**
4. 等待 Android Studio 自动同步 Gradle 并下载依赖（首次打开可能需要 5~15 分钟，取决于网络速度）

### 步骤 2：同步 Gradle

如果 Android Studio 没有自动同步，点击菜单栏：

```
File → Sync Project with Gradle Files
```

或点击工具栏中的 🔄 图标。

### 步骤 3：构建项目

```
Build → Make Project   (快捷键：Ctrl + F9)
```

或在 Terminal 中执行：

```bash
./gradlew assembleDebug
```

（Windows 上请使用 `gradlew.bat assembleDebug`）

### 步骤 4：运行到设备或模拟器

#### A. 使用真机（推荐，NotificationListenerService 在真机上体验更好）

1. 开启手机的 **开发者选项** 和 **USB 调试**
   - 设置 → 关于手机 → 连续点击"版本号"7次开启开发者模式
   - 返回设置 → 系统 → 开发者选项 → 开启"USB调试"
2. 用 USB 数据线连接电脑和手机
3. 在 Android Studio 工具栏中选择你的设备
4. 点击 **Run** 按钮（▶️）或按 `Shift + F10`

#### B. 使用模拟器

1. 打开 **Device Manager**（设备管理器）
2. 点击 **Create Device**，选择一款手机（如 Pixel 7）
3. 下载并选择 **Android 14/15 (API 35)** 系统镜像
4. 启动模拟器
5. 点击 **Run** 按钮运行

> ⚠️ **注意**：模拟器上测试通知捕获时，你需要从其他应用发送通知到模拟器才能看到捕获效果。

## 五、首次使用的必要设置

### 通知访问权限（消息捕获必需）

应用安装后，**必须手动授予"通知访问权限"**，否则无法捕获消息：

1. 打开应用，切换到「消息」Tab
2. 点击右上角 **设置（齿轮）图标**
3. 进入 **系统设置 → 通知 → 通知访问权限**（不同手机路径可能略有不同）
4. 找到 **"应用消息捕获服务"**，将其开关打开
5. 返回应用，此时所有新收到的通知将会被自动捕获

### 电池优化白名单（重要！）

**国内厂商 ROM（小米、华为、OPPO、vivo 等）会积极杀死后台服务**。为避免应用被清理后无法继续工作，请进行以下设置：

1. **关闭电池优化**：首次启动应用时会弹出系统对话框，请点击"允许"忽略电池优化
2. **允许自启动**：进入系统设置 → 应用管理 → 应用消息捕获 → 自启动管理 → 允许
3. **锁定后台**：打开最近任务列表，将本应用卡片下拉锁定，防止被一键清理

> 如果通知服务意外断开，应用主界面顶部会出现黄色提示条，点击"重新激活"即可手动恢复。

## 六、功能说明

### 🔔 消息 Tab

| 功能 | 说明 |
|------|------|
| 实时捕获 | 自动捕获所有应用通知（微信、QQ、短信、邮件等） |
| 本地存储 | 所有消息保存在本地 Room 数据库，不上传云端 |
| 消息列表 | 按时间倒序，按日期分组，显示应用名、标题、内容、时间 |
| 搜索过滤 | 支持按应用名或消息内容搜索，按应用筛选/屏蔽 |
| 已读标记 | 点击卡片标记已读，未读卡片有高亮背景和小红点 |
| 消息导出 | 支持导出为 JSON / CSV 格式 |
| 服务状态 | 顶部实时提示服务运行状态，支持一键重新激活 |
| 自动启动 | 注册开机广播，重启后服务自动恢复 |

### 💰 记账 Tab

| 功能 | 说明 |
|------|------|
| 自动记账 | 监听支付类通知，自动识别金额并分类入账 |
| 手动记账 | 支持选择日期（补记/预记），当天记录当前实际时间 |
| 分类管理 | 12 种支出分类 + 8 种收入分类，每项配独立图标和颜色 |
| 分类迁移 | 旧版分类名自动映射到新版，支持一键批量迁移 |
| 周期账单 | 固定支出设为周期性自动记账（每日/每周/双周/每月/每年） |
| 日期选择 | Material3 DatePicker，未来日期自动禁用 |
| 收支报表 | 按周/月/年查看收支趋势，环形图展示分类构成 |
| 分类详情 | 点击分类可下钻查看该分类的详细账单列表 |
| 多选删除 | 长按进入多选模式，批量删除账单 |

### 🎂 生日 Tab

| 功能 | 说明 |
|------|------|
| 生日记录 | 支持阳历/农历，自动计算倒计时和天数 |
| 到期提醒 | 生日当天及提前 N 天推送通知（AlarmManager） |
| 桌面小组件 | Glance AppWidget 展示近期生日，定时自动刷新 |
| 导入导出 | JSON 格式批量备份与恢复，导入后界面即时刷新 |
| 分页加载 | 列表分页滚动加载，大量数据流畅展示 |
| 编辑删除 | 支持新建、编辑、单选删除和多选批量删除 |

## 七、数据库迁移说明

当前数据库版本为 **v11**，包含以下迁移链：

| 迁移 | 说明 |
|------|------|
| v1 → v8 | 初始建表（通知、账单等基础表） |
| v8 → v9 | 添加生日表 |
| v9 → v10 | 分类名称迁移（旧短名 → 新全名，如"餐饮"→"餐饮美食"） |
| v10 → v11 | 创建周期账单表（recurring_bills） |

所有迁移均在 `AppDatabase.kt` 中定义，Room 会自动按链执行。

## 八、如何安装 APK 到手机

### 方式一：通过 Android Studio 直接安装

按上述"步骤 4"点击 Run 即可自动编译并安装 Debug APK。

### 方式二：通过命令行编译

```bash
# 进入项目根目录
cd AppMessageCapture

# 编译 Debug APK
./gradlew assembleDebug      # Windows: gradlew.bat assembleDebug

# 安装到已连接的设备
./gradlew installDebug        # Windows: gradlew.bat installDebug
```

编译后的 Debug APK 路径：
```
app/build/outputs/apk/debug/app-debug.apk
```

### 方式三：生成 Release APK

1. 在 Android Studio 中选择：`Build → Generate Signed App Bundle / APK...`
2. 选择 **APK**，点击 **Next**
3. 创建或选择密钥库文件，选择 `release` 构建类型
4. 生成的 APK 位于 `app/release/app-release.apk`
5. 传输到手机安装即可

## 九、模拟支付通知自测（调试专用）

无需真实消费，即可测试自动记账功能是否正常。原理：debug 构建内置一个
`SimulateNotificationReceiver`（`app/src/debug/`，release 包不含），接收 adb
广播后把合成的通知喂给真实捕获管线（应用过滤 → 金额解析 → 去重 → 入库 → 记账提醒）。

### 前置条件

1. 已安装 **debug 构建**（`./gradlew :app:installDebug`）
2. 已开启「通知访问权限」
3. adb 可用（脚本会自动从 `local.properties` 定位 SDK，无需配置 PATH）

### 用法

```bash
# 格式: ./scripts/simulate_notification.sh <应用别名|包名> <标题> <内容>

# 云闪付
./scripts/simulate_notification.sh unionpay "支付助手：付款成功" \
  "您尾号为5580的银行卡于19日18时38分消费18.80元。"

# 中国农业银行
./scripts/simulate_notification.sh abc "中国农业银行" \
  "您尾号为7374的农行借记卡于08月13日18:45发生一笔支出17.66元，详情请点击。"

# 美团退款（自动记为收入账单，分类归入「退款返现」）
./scripts/simulate_notification.sh meituan "退款通知" \
  "您有一笔90.00元的退款，点击查看详情！"
```

应用别名：`wechat`(微信) `alipay`(支付宝) `meituan`(美团) `waimai`(美团外卖)
`unionpay`(云闪付) `abc`(农业银行)
`dss`(抖省省)，其他值按原样作为包名使用。

### 结果解读

- 广播输出 `result=-1` 表示注入成功，捕获成功后手机会弹「记账成功」通知
- 通知为**常驻**（不会被系统回收、不可下滑清除），带三个按钮：**去查看**（进
  App 记账 Tab 并清除通知）、**完善账单**（双栏弹窗：左分类/右扣款平台一次
  保存两项，平台变更联动余额）、**删除**（事务删除该笔账单并回滚已对账
  平台余额，通知随之清除）——不进 App 即可处理每笔账单。
  注：顶部悬浮横幅几秒后收起是系统行为，收起后通知仍在通知栏等待处理
- `result=1` 参数缺失；`result=2` 通知监听服务未连接（先开通知使用权）
- 脚本会自动先把 App 拉到前台：MIUI/HyperOS 的 Greezer 会延迟发给缓存进程的广播
- 同一消息 60 秒内重复发送会被去重规则忽略，测试时请变换金额或标题

## 十、屏幕记账（无障碍：京东支付成功页 + 淘宝闪购订单页）

解决「付款了但应用不发通知 / 通知里没有金额」的场景（如京东 App 付款后
只有「支付成功」页面；淘宝闪购支付完成落在订单详情页，均无系统通知）。
原理：无障碍服务 `PaymentScreenAccessibilityService` 监视支付 App 的
窗口切换事件，识别到对应页面后从窗口文本中提取支付信息并入账。

### 工作方式

1. 事件投递收紧到监视名单（`SupportedPaymentApps.screenWatchPackages`，
   与 `res/xml/payment_screen_accessibility_config.xml` 的 `packageNames`
   保持一致，新增应用两处同步改）：目前为京东 `com.jingdong.app.mall`、
   淘宝 `com.taobao.taobao`（淘宝内闪购频道）与独立淘宝闪购 App `me.ele`；
   事件类型仅窗口切换（捕获目标是支付完成后自动弹出的订单页，历史订单
   「下单时间」折叠不命中门槛，浏览历史订单零动作）；
2. 页面判定按包名路由（`PaymentScreenParsing.isCapturePage`）：
   - 京东：窗口文本含「支付成功/付款成功」（一次性成功页，普通页面零动作）；
   - 淘宝闪购（两个入口统一）：含「闪购」+「实付」+「下单时间」的支付完成
     订单页（`TaobaoShangouParsing`）——「闪购」区分普通淘宝订单页、
     「下单时间」区分订单列表页（无该行）与历史订单详情页（该行折叠在
     「订单信息」里不可见）；
3. 金额提取：
   - 京东：只认「支付/付款」动词后紧跟的数字（含「支付成功¥30.38」变体），
     排除「领5元红包」「满6减5」「¥3 到手价」等营销金额；
   - 淘宝闪购：只认「实付」后紧跟的货币金额；账单标题=商户名+实付金额，
     账单时间=页面「下单时间」（每单唯一）。幂等：同一订单再次打开由内容
     去重（时间窗口锚定下单时间）丢弃；历史订单「下单时间」折叠不命中
     门槛，天然不会重复入账；
4. 入库走 `BillIngestor`（与通知捕获共用去重与跨 App 合并）：同一笔支付
   「渠道通知 + 商户页」只记一条账（淘宝闪购权重 90 高于支付宝/微信 50，
   闪购订单页会吸收支付渠道先记的那条）；方向固定为支出。

### 开启方法（必须手动）

消息页顶栏「屏幕记账权限」图标（未开启时带角标）→ 系统无障碍设置 →
「屏幕记账（支付成功页）」→ 开启。

> HyperOS/MIUI 注意：侧载应用默认禁止开启无障碍，需先在
> 「应用信息 → 右上角菜单 → 允许受限设置」后再回到无障碍设置开启。
> 另外 HyperOS 禁止 adb 写入无障碍开关（`WRITE_SECURE_SETTINGS` 加固），
> 无法用命令行代替手动开启。

### 自测（屏幕模式模拟）

```bash
# 模拟京东支付成功页（效果等同真实付款后无障碍读到窗口文本）
./scripts/simulate_notification.sh jdapp "支付成功" \
  "京东支付¥30.38，共优惠¥0.02" --screen

# 模拟淘宝闪购支付完成订单页（淘宝内/独立 App me.ele 同一套解析，
# 多行内容用 $'\n' 分隔；门槛要求含「下单时间」行）
./scripts/simulate_notification.sh com.taobao.taobao "闪购 袁记云饺(文汇路店)" \
  $'价格明细\n总优惠¥33 实付¥18.98\n下单时间 2026-08-30 18:16:14.118' --screen
```

- `result=-1` 注入成功；`result=3` 屏幕记账服务未开启
- 京东模拟应记一笔 30.38 元支出账单；闪购模拟应记一笔标题为
  「袁记云饺(文汇路店) 实付¥18.98」的支出账单，时间落在 2026-08-30 18:16；
  重复发送会被防抖 + 去重忽略
- 对应单元测试：`PaymentScreenParsingTest`（京东，取自真实付款截图）、
  `TaobaoShangouParsingTest`（闪购，取自真实订单页截图）

## 十一、旧版数据迁移（换包名/改名后）

应用曾从旧包名 `com.aifactory.appmessagecapture` 改为 `com.zhaojin.billcatch`，
系统视为两个独立应用，旧 app 的数据不会自动带过来；且导入导出功能是改包名
之后才加的，旧 app 里没有导出入口。两条迁移路径按需选用。

### 方案 A：桥接包原地升级（推荐，目标手机无需电脑）

原理：用当前完整代码（已含导入导出功能）以**旧包名**打一个「桥接包」APK。
它 versionCode 更高、与旧 app 签名一致（同一台电脑的 debug 密钥），在那台
手机上点击安装即**原地升级**旧 app——数据全部保留，且旧 app 补上了导出
功能；再从旧 app 导出 JSON、新 app 导入即可。

1. 在电脑上构建桥接包（一次构建，可发给任意多台设备）：

   ```bash
   ./gradlew :app:assembleDebug -PlegacyPackage=true
   # 产物: app/build/outputs/apk/debug/AppMessageCapture-v*-legacy.apk
   ```

2. 把 APK 通过微信/QQ/网盘发到目标手机，点击安装（系统显示为"更新"而非
   "安装"，数据保留）。**前提：那台手机上的旧 app 也是本机 debug 签名装的**；
   若提示签名冲突/无法安装，说明签名不同，改走方案 B；
3. 打开升级后的旧 app（消息页顶栏已有导出图标）→ 导出 JSON → 保存或分享；
4. 打开新包名 app → 导入该 JSON；
5. 迁移完成后建议**停用/卸载旧 app**：旧 app 的通知使用权在系统里仍生效，
   留着会继续捕获消息，与新版重复记账。

> 本机实测：旧包 app（v2.2.3/vc2）安装桥接包后原地升级为 v2.2.5，
> 历史数据完好、导出入口可用。

### 方案 B：adb run-as 直拷数据库（有电脑时最快）

用 adb `run-as` 把旧 app 私有目录里的 Room 数据库（账单/消息/生日/预算/
平台账户）和 SharedPreferences（主题色等）直接拷进新 app。`run-as` 只要求
两端都是 debug 构建，**不校验签名**（方案 A 签名冲突时的兜底）。

1. 设备连接电脑，确认 adb 可用（多台设备时 `ANDROID_SERIAL=<serial>` 指定）；
2. 安装新 app：`./gradlew :app:installDebug`（**先别打开它**；打开过也没关系，脚本会强停再覆盖）；
3. 先演练一遍，验证通路且不覆盖任何数据：

   ```bash
   ./scripts/migrate_old_app.sh --dry-run
   ```

4. 正式迁移（会打印新 app 原数据备份位置，回滚就靠它）：

   ```bash
   ./scripts/migrate_old_app.sh --yes
   ```

5. 脚本会自动拉起新 app，首次打开时 Room 自动把旧库结构升级到当前版本
   （迁移链完整，覆盖旧版任意历史版本）。

### 前置条件与注意

- 旧包名默认 `com.aifactory.appmessagecapture`：方案 B 可用
  `--old/--new/--db` 覆盖，不想迁移偏好设置加 `--no-prefs`；
- `--yes` 跳过交互确认（会覆盖新 app 现有同名数据）；覆盖前自动备份到
  项目根目录 `migration_backup_<时间戳>/`（已加入 .gitignore，勿提交）；
- 方案 B 实测记录：dry-run 探针哈希校验一致，真实数据未动；
- 旧 app 是 release 构建（非 debuggable）时：方案 B 的 run-as 不可用；
  方案 A 需用**原 release 签名密钥**打桥接包才能覆盖安装，两者都不满足
  （无原签名密钥且无 root）则无法迁移。

## 十二、常见问题

**Q1：安装后没有捕获到任何消息？**
> 必须前往系统设置开启"通知访问权限"。仅安装应用是不够的。

**Q2：后台清理后无法捕获，必须重新开关权限？**
> 请关闭电池优化并锁定后台。新版已增加"重新激活"按钮，点击顶部黄色提示条即可恢复。

**Q3：会不会泄露隐私？**
> 所有数据仅存储在手机本地 Room 数据库中，不联网、不上传、不涉及任何远程服务器。

**Q4：手动记账时间显示 00:00？**
> 选择今天时会自动记录当前实际时间；选择其他日期时默认记录中午 12:00。

**Q5：周期账单没有自动执行？**
> 周期账单通过 WorkManager 定时执行，请确保应用未被电池优化限制。首次添加周期账单后会在应用启动时自动注册 Worker。

**Q6：导入生日后界面没有更新？**
> 已在最新版本中修复，导入完成后会自动刷新列表数据、桌面小组件和闹钟调度。

**Q7：如何彻底删除数据？**
> 卸载应用即可，所有本地数据会一并清除。

---

如有任何问题，欢迎通过 Android Studio 的 Logcat 查看运行日志进行排查。
