# 应用消息捕获 — 完整运行清单

## 一、项目简介

本项目是一个完整的 Android 应用程序，名为 **应用消息捕获**。它通过 Android 系统级 `NotificationListenerService` 捕获手机上所有应用收到的通知消息，并使用 `Room` 数据库存储在本地。UI 采用 `Jetpack Compose` + `Material Design 3` 构建，配色以绿色/青色为主，健康积极。

## 二、项目结构

```
AppMessageCapture/
├── build.gradle.kts                          // 项目级构建配置
├── settings.gradle.kts
├── gradle.properties
├── gradle/wrapper/gradle-wrapper.properties  // Gradle 8.7
├── 运行清单.md                                // 本文件
└── app/
    ├── build.gradle.kts                      // 模块级构建配置
    ├── proguard-rules.pro
    └── src/main/
        ├── AndroidManifest.xml
        ├── java/com/aifactory/appmessagecapture/
        │   ├── MainActivity.kt
        │   ├── AppMessageCaptureApplication.kt
        │   ├── data/
        │   │   ├── NotificationEntity.kt
        │   │   ├── NotificationDao.kt
        │   │   └── AppDatabase.kt
        │   ├── service/
        │   │   ├── MessageCaptureService.kt
        │   │   └── BootReceiver.kt
        │   ├── ui/
        │   │   ├── MainScreen.kt
        │   │   ├── NotificationViewModel.kt
        │   │   └── theme/
        │   │       ├── Color.kt
        │   │       ├── Theme.kt
        │   │       └── Type.kt
        │   └── utils/
        │       └── NotificationServiceHelper.kt
        └── res/
            ├── values/strings.xml
            ├── values/colors.xml
            ├── values/themes.xml
            ├── drawable/ic_launcher_background.xml
            ├── drawable/ic_launcher_foreground.xml
            ├── mipmap-anydpi-v26/ic_launcher.xml
            ├── mipmap-anydpi-v26/ic_launcher_round.xml
            └── xml/
                ├── data_extraction_rules.xml
                └── backup_rules.xml
```

## 三、依赖环境

### 必需软件

1. **Android Studio**（推荐最新稳定版，如 Android Studio Koala 或更新版本）
   - 下载地址：https://developer.android.com/studio
2. **JDK 17**（或更高版本）
   - Android Studio 自带 bundled JDK，通常无需单独安装
3. **Android SDK**
   - 通过 Android Studio 的 SDK Manager 安装
   - 必需组件：
     - Android SDK Platform 35
     - Android SDK Build-Tools 35
     - Android Emulator（如需在模拟器上运行）

### 系统要求

- **操作系统**：Windows 10/11、macOS 或 Linux
- **内存**：至少 8GB RAM（推荐 16GB）
- **磁盘空间**：至少 10GB 可用空间

## 四、如何运行该项目

### 步骤 1：打开项目

1. 启动 **Android Studio**
2. 选择 **Open**（打开现有项目）
3. 导航到本项目的根目录 `AppMessageCapture/`，点击 **OK**
4. 等待 Android Studio 自动同步 Gradle 并下载依赖（首次打开可能需要 5~15 分钟，取决于网络速度）

### 步骤 2：同步 Gradle

如果 Android Studio 没有自动同步，点击菜单栏：

```
File → Sync Project with Gradle Files
```

或点击工具栏中的 🔄 图标。

### 步骤 3：构建项目

```
Build → Make Project   (快捷键：Ctrl + F9)
```

或在 Terminal 中执行：

```bash
./gradlew assembleDebug
```

（Windows 上请使用 `gradlew.bat assembleDebug`）

### 步骤 4：运行到设备或模拟器

#### A. 使用真机（推荐，因为 NotificationListenerService 在真机上体验更好）

1. 开启手机的 **开发者选项** 和 **USB 调试**
   - 设置 → 关于手机 → 连续点击“版本号”7次开启开发者模式
   - 返回设置 → 系统 → 开发者选项 → 开启“USB调试”
2. 用 USB 数据线连接电脑和手机
3. 在 Android Studio 工具栏中选择你的设备
4. 点击 **Run** 按钮（▶️）或按 `Shift + F10`

#### B. 使用模拟器

1. 打开 **Device Manager**（设备管理器）
2. 点击 **Create Device**，选择一款手机（如 Pixel 7）
3. 下载并选择 **Android 14/15 (API 35)** 系统镜像
4. 启动模拟器
5. 点击 **Run** 按钮运行

> ⚠️ **注意**：模拟器上测试通知捕获时，你需要从其他应用（如浏览器、邮件）发送通知到模拟器，才能看到捕获效果。

## 五、首次使用的必要设置

应用安装后，**必须手动授予“通知访问权限”**，否则无法捕获消息：

1. 打开应用
2. 点击右上角 **设置（齿轮）图标**，或系统会提示你前往设置
3. 进入 **系统设置 → 通知 → 通知访问权限**（不同手机路径可能略有不同）
4. 找到 **“应用消息捕获服务”**，将其开关打开
5. 返回应用，此时所有新收到的通知将会被自动捕获并显示在列表中

> 你也可以手动前往：设置 → 应用和通知 → 特殊应用权限 → 通知访问权限 → 开启本应用。

### 电池优化白名单（重要！）

**国内厂商 ROM（小米、华为、OPPO、vivo 等）会 aggressively 杀死后台服务**。为避免应用被清理后无法继续捕获消息，请进行以下设置：

1. **关闭电池优化**：首次启动应用时会弹出系统对话框，请点击“允许”忽略电池优化。
2. **允许自启动**：进入系统设置 → 应用管理 → 应用消息捕获 → 自启动管理 → 允许。
3. **锁定后台**：打开最近任务列表（多任务界面），将本应用卡片**下拉锁定**（或点击锁图标），防止被一键清理。

> 如果服务意外断开，应用主界面顶部会出现黄色提示条，点击“重新激活”即可手动恢复，无需重新开关权限。

## 六、核心 Bug 修复说明（后台清理后服务断开）

**问题现象**：授权通知权限后正常捕获，但从最近任务划掉应用后，服务停止工作，必须重新关闭/开启权限才能恢复。

**根本原因**：
- NotificationListenerService 随应用进程运行，进程被杀后服务断开。
- 国内 ROM 的电池优化策略会阻止服务自动重连。

**修复措施**：
1. **服务状态追踪**：`MessageCaptureService` 增加了 `isConnected` 静态标志，实时反映系统是否已绑定服务。
2. **状态轮询**：`MainScreen` 每 2 秒检测一次服务连接状态。
3. **手动重连**：当检测到服务已授权但未连接时，顶部显示黄色提示条，点击“重新激活”调用 `NotificationListenerService.requestRebind()` 强制系统重新绑定。
4. **电池优化白名单**：首次启动引导用户关闭电池优化，降低被系统杀死的概率。
5. **开机自启**：保留 `BootReceiver`，设备重启后自动恢复服务监听。

## 七、如何安装 APK 到手机

### 方式一：通过 Android Studio 直接安装（开发调试）

按上述“步骤 4”点击 Run 即可自动编译并安装 Debug APK。

### 方式二：生成 Release APK 并手动安装

1. 在 Android Studio 中选择：
   ```
   Build → Generate Signed App Bundle / APK...
   ```
2. 选择 **APK**，点击 **Next**
3. 创建或选择一个 **密钥库（Key Store）** 文件用于签名
4. 选择 `release` 构建类型，点击 **Finish**
5. 生成的 APK 位于：
   ```
   app/release/app-release.apk
   ```
6. 将 APK 传输到手机，点击安装即可
   - 如果系统提示“未知来源”，请在设置中允许安装未知应用

### 方式三：通过命令行编译

```bash
# 进入项目根目录
cd AppMessageCapture

# 编译 Debug APK
./gradlew assembleDebug

# 安装到已连接的设备
./gradlew installDebug
```

编译后的 Debug APK 路径：
```
app/build/outputs/apk/debug/app-debug.apk
```

## 八、功能说明

| 功能 | 说明 |
|------|------|
| 实时捕获 | 自动捕获手机上所有应用的通知（微信、QQ、短信、邮件等） |
| 本地存储 | 所有消息保存在本地 Room 数据库，不上传云端 |
| 消息列表 | 按时间倒序展示，显示应用名、标题、内容、接收时间 |
| 搜索过滤 | 顶部搜索栏支持按应用名或消息内容搜索 |
| 已读标记 | 点击卡片可标记为已读（未读卡片有高亮背景和小红点） |
| 一键清空 | 右下角悬浮按钮可清空所有记录 |
| 服务状态 | 顶部实时提示服务运行状态，支持一键重新激活 |
| 自动启动 | 注册开机广播，重启后服务会自动恢复（需已授权） |

## 九、技术栈总结

| 技术 | 用途 |
|------|------|
| Kotlin | 开发语言 |
| Jetpack Compose | 声明式 UI 构建 |
| Material Design 3 | 现代化 UI 组件与主题 |
| Room | 本地数据持久化 |
| Kotlin Coroutines / Flow | 异步数据流与响应式 UI |
| NotificationListenerService | 系统级通知监听 |
| ViewModel | UI 状态管理 |

## 十、常见问题

**Q1：为什么安装后没有捕获到任何消息？**
> 必须前往系统设置开启“通知访问权限”。仅安装应用是不够的。

**Q2：后台清理后无法捕获，必须重新开关权限？**
> 请按“步骤五”关闭电池优化并锁定后台。新版已增加“重新激活”按钮，点击顶部黄色提示条即可恢复，无需再去设置里开关权限。

**Q3：会不会泄露隐私？**
> 所有数据仅存储在手机本地 Room 数据库中，不联网、不上传、不涉及任何远程服务器。

**Q4：某些应用的消息没有显示？**
> 部分应用（如部分银行类、支付类 App）的通知可能被系统加密或受保护，无法被第三方读取。

**Q5：如何彻底删除数据？**
> 卸载应用即可，所有本地数据会一并清除。

---

如有任何问题，欢迎通过 Android Studio 的 Logcat 查看运行日志进行排查。
